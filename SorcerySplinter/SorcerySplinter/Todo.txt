

■どんな機能が欲しいか。

・スニペット化する前のコードを書き溜める機能
（自分用）仕事中にどっかのテキストファイルに書くので、そのファイルを1つだけ記憶、表示。

・作ったスニペットを読み込む
・ディレクトリを指定して一覧表示、クリックしたら詳細表示

・保存用リポジトリとVSのディレクトリに保存、この2つはペアで複数登録する。（保存先を複数登録でいいか。）
→C#のディレクトリ：VSのC#スニペット・リポジトリのC#の所

■画面
タブで切り替える。

・Home
自分用なので使い方を思い出せるように簡単に記述する


・設定
自分用モードトグルボタンを作る。

（自分用）書き溜めテキストファイルの場所。
今までのスニペットをロードする時の参照パス
共通の入力項目
→スニペットに載せる名前など

・保存先登録
保存先：リポジトリ用と、VisualStudio用の2つ必要。だけど言語別も欲しいなあ。というわけで。
→言語選択（すぐには使わないけど今後用）
→保存名（一覧から選択する時に表示）
→保存パス（VisualStudioのパスがデフォルト入力される機能付き）



・入力画面
1画面にスニペット作成に必要な情報をすべて入力する



・今までのスニペットをロードする
指定フォルダ以下の一覧リストから選択すると、プレビューが出てくれる感じ。ロードすると入力画面に遷移する。
言語別にまとめるため、フォルダごとに分ける。プルダウンでフォルダ選択。




・（自分用）書き溜め
指定したテキストファイルを表示
書き終わったものは削除したいので編集と保存機能も。

------------------------------------ こっちは別のソフトの話 ------------------------------------
・旧プログラムからの移植方針を決定
旧プログラムにあった画面はこれだけ。
<MenuItem Header="_Home" Command="{Binding ToHome}"/>
<MenuItem Header="_Dropbox設定" Command="{Binding ToDropboxSettings}"/>
<MenuItem Header="_接続先登録" Command="{Binding ToConnectionRegister}"/>
<MenuItem Header="_メタデータ生成" Command="{Binding ToGenerate}"/>
<MenuItem Header="_テンプレート編集" Command="{Binding ToTemplateEdit}"/>
<MenuItem Header="_汎用入力" Command="{Binding ToGenetalInput}"/>
<MenuItem Header="_ソース生成" Command="{Binding ToSourceGenerate}"/>
<MenuItem Header="_出力結果" Command="{Binding ToOutputResult}"/>

・旧プログラムからの移植
※と思ったけど、旧プログラムは結局DBを直接読んでそこからコードを作るので、全く別のソフトだね…。旧プログラム消しちゃダメだ。
消すならライブラリ化して消すとか、Excel出力機能作るとか。
旧プログラムは、消す前にサンプルデータを回収すること。DropBoxにあるかも？

・サンプルデータの作成


■画面
タブで切り替える。

・Home
自分用なので使い方を思い出せるように簡単に記述する

・設定

・

・







