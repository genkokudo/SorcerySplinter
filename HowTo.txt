// どんなことをテストするのか？

// ViewModelに関してテストを行う
// サービスはモック（作成したサービスもテストを作成する）

デフォルトに存在していたViewAViewModelFixtureに書いてあったテスト（Fixture:器具、固定具なので、テストを定義することで入出力を固定しようという意図なのだと思う）

// 1個目
// GetMessage()が1回呼び出されたことを確認する
// メッセージがモックに設定した通りであること（サービスで得た文字列に何も変更が入っていないこと）

// 2個目
// コンストラクタによってプロパティが変更されていること