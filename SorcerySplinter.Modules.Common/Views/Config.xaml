<UserControl x:Class="SorcerySplinter.Modules.Common.Views.Config"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:SorcerySplinter.Modules.Common.Views"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True" >
    <Grid>
        <StackPanel>
            <TextBlock Text="設定" HorizontalAlignment="Left" VerticalAlignment="Top" />

            <Label Content="名前" Margin="0,30,0,0" />
            <TextBox Text="{Binding Author}" ToolTip="スニペットの作者として表記する名前です。" materialDesign:HintAssist.Hint="G.Iwatobi" materialDesign:HintAssist.HelperTextFontSize="16" />

            <Label Content="保存場所（VisualStudio用）" Margin="0,30,0,0" />
            <Label Content="説明" Margin="0,30,0,0" />
            <StackPanel Orientation="Horizontal">
                <TextBox Text="{Binding SnippetDirectoryVs}" ToolTip="VisualStudioのスニペットディレクトリ。これを指定すると自動でVisualStudioにスニペット導入できます。" materialDesign:HintAssist.Hint="C:\Users\(UserName)\Documents\Visual Studio 2019\Code Snippets" materialDesign:HintAssist.HelperTextFontSize="16" />
                <Button Content="参照" Command="{Binding VsFolderCommand}" CommandParameter="VS" HorizontalAlignment="Right" VerticalAlignment="Center" Height="24"/>
            </StackPanel>

            <Label Content="保存場所（任意）" Margin="0,30,0,0" />
            <StackPanel Orientation="Horizontal">
                <TextBox Text="{Binding SnippetDirectory}" ToolTip="スニペット保存場所です。設定しなくても良いです。" materialDesign:HintAssist.Hint="C:\Users\(UserName)\source\repos\(ProjectName)" materialDesign:HintAssist.HelperTextFontSize="16" />
                <Button Content="参照" Command="{Binding CommonFolderCommand}" CommandParameter="Common" HorizontalAlignment="Right" VerticalAlignment="Center" Height="24"/>
            </StackPanel>
            
        </StackPanel>
        <Button Content="保存" Command="{Binding SaveCommand}" HorizontalAlignment="Right" VerticalAlignment="Bottom"/>
    </Grid>
</UserControl>
